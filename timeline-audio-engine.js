(()=>{
  const q=s=>document.querySelector(s);
  const clamp=(v,min,max)=>Math.max(min,Math.min(max,v));
  const timing=()=>window.CheerAudioTiming||null;
  const dsp=()=>window.CheerMixDSP||null;
  const bufferCache=new Map(),decodePromises=new Map(),activeSources=new Set();
  let ctx=null,masterGain=null,running=false,starting=false,timelineAnchor=0,contextAnchor=0,generation=0;

  function ensure(){if(!state.audioTimeline)state.audioTimeline={clips:[],zoom:1,snap:'beat'};state.audioTimeline.clips=Array.isArray(state.audioTimeline.clips)?state.audioTimeline.clips:[];if(!state.mixSettings)state.mixSettings={autoDuck:true,duckDb:-7,duckAttack:.08,duckRelease:.18}}
  function audioContext(){if(!ctx){const C=window.AudioContext||window.webkitAudioContext;if(!C)throw new Error('Web Audio API ei ole käytettävissä tässä selaimessa.');ctx=new C({latencyHint:'interactive'});masterGain=ctx.createGain();masterGain.gain.value=1;masterGain.connect(ctx.destination)}return ctx}
  function presentedContextTime(){if(!ctx)return 0;try{const ts=ctx.getOutputTimestamp?.();if(ts&&Number.isFinite(ts.contextTime)&&ts.contextTime>0)return ts.contextTime}catch{}return ctx.currentTime}
  function trackFor(c){return(state.tracks||[]).find(t=>t.name===c.sourceName)}
  function rateFor(c){if(dsp()?.rateForClip)return dsp().rateForClip(c,state.trackAnalysis||{},state.targetBpm||147);if(c.type!=='music')return 1;const bpm=Number(state.trackAnalysis?.[c.sourceName]?.bpm||state.trackAnalysis?.[c.sourceName]?.autoBpm),target=Number(state.targetBpm)||147;return Number.isFinite(bpm)&&bpm>0?clamp(target/bpm,.5,2):1}
  function maxDurationFor(c,offset=c.sourceOffset||0){const t=trackFor(c),rate=rateFor(c);return Number.isFinite(t?.duration)&&t.duration>0?Math.max(0,(t.duration-Math.max(0,offset))/Math.max(.0001,rate)):Infinity}
  function trackKey(t){return`${t.name}::${t.size||''}::${t.duration||''}::${t.url||''}`}

  async function decodeTrack(t){if(!t?.url)throw new Error(`Audiotiedosto puuttuu: ${t?.name||'tuntematon'}`);const key=trackKey(t);if(bufferCache.has(key))return bufferCache.get(key);if(decodePromises.has(key))return decodePromises.get(key);const promise=(async()=>{const ac=audioContext(),bytes=await fetch(t.url).then(r=>{if(!r.ok)throw new Error(`Audiota ei voitu lukea: ${t.name}`);return r.arrayBuffer()}),buffer=await ac.decodeAudioData(bytes.slice(0));bufferCache.set(key,buffer);decodePromises.delete(key);return buffer})().catch(err=>{decodePromises.delete(key);throw err});decodePromises.set(key,promise);return promise}
  async function preloadFor(playFrom){ensure();const tracks=new Map();for(const c of state.audioTimeline.clips){const end=(Number(c.start)||0)+Math.max(0,Number(c.duration)||0);if(end<=playFrom)continue;const t=trackFor(c);if(t?.url)tracks.set(trackKey(t),t)}const results=await Promise.allSettled([...tracks.values()].map(decodeTrack)),failed=results.find(r=>r.status==='rejected');if(failed)throw failed.reason}

  function clipEnvelopeAt(c,t){if(dsp()?.clipEnvelopeAt)return dsp().clipEnvelopeAt(c,t);if(timing()?.envelopeAt)return timing().envelopeAt(c,t);const start=Number(c.start)||0,dur=Math.max(0,Number(c.duration)||0),local=t-start;if(local<0||local>dur)return 0;const base=clamp(Number.isFinite(Number(c.volume))?Number(c.volume):1,0,1),fi=Math.max(0,Number(c.fadeIn)||0),fo=Math.max(0,Number(c.fadeOut)||0);let env=1;if(fi>0&&local<fi)env=Math.min(env,local/fi);const remain=Math.max(0,dur-local);if(fo>0&&remain<fo)env=Math.min(env,remain/fo);return clamp(base*env,0,1)}
  function legacyClipPoints(c,start,end){const cs=Number(c.start)||0,ce=cs+Math.max(0,Number(c.duration)||0),fi=Math.max(0,Number(c.fadeIn)||0),fo=Math.max(0,Number(c.fadeOut)||0);return[...new Set([start,end,cs,cs+fi,ce-fo,ce].filter(t=>t>=start&&t<=end))].sort((a,b)=>a-b).map(t=>[t,clipEnvelopeAt(c,t)])}
  function legacyVoiceWindows(){ensure();if(state.mixSettings.autoDuck===false)return[];const attack=clamp(Number(state.mixSettings.duckAttack)||.08,.01,1),release=clamp(Number(state.mixSettings.duckRelease)||.18,.01,2),duck=Math.pow(10,clamp(Number(state.mixSettings.duckDb)||-7,-18,0)/20);return state.audioTimeline.clips.filter(v=>v.type==='voice'&&(Number.isFinite(Number(v.volume))?Number(v.volume):1)>0).map(v=>{const s=Number(v.start)||0,d=Math.max(0,Number(v.duration)||0);return{start:s-attack,voiceStart:s,voiceEnd:s+d,end:s+d+release,attack,release,duck}})}
  function duckFactorAt(t){if(dsp()?.duckFactorAt)return dsp().duckFactorAt(t,state.audioTimeline.clips,state.mixSettings);if(state.mixSettings.autoDuck===false)return 1;let f=1;for(const w of legacyVoiceWindows()){if(t<w.start||t>w.end)continue;let x=w.duck;if(t<w.voiceStart)x=1-(1-w.duck)*clamp((t-w.start)/Math.max(.0001,w.attack),0,1);else if(t>w.voiceEnd)x=w.duck+(1-w.duck)*clamp((t-w.voiceEnd)/Math.max(.0001,w.release),0,1);f=Math.min(f,x)}return f}
  function legacyDuckPoints(c,start,end){if(c.type!=='music'||state.mixSettings.autoDuck===false)return[[start,1],[end,1]];const times=[start,end];for(const w of legacyVoiceWindows())times.push(w.start,w.voiceStart,w.voiceEnd,w.end);return[...new Set(times.filter(t=>t>=start&&t<=end))].sort((a,b)=>a-b).map(t=>[t,duckFactorAt(t)])}
  function scheduleParam(param,points,when,timelineStart){if(dsp()?.scheduleParam){dsp().scheduleParam(param,points,when,timelineStart);return}if(!points?.length)return;param.cancelScheduledValues(when);points.forEach((p,i)=>{const ct=when+(p[0]-timelineStart),v=Math.max(.000001,p[1]);if(i===0)param.setValueAtTime(v,ct);else param.linearRampToValueAtTime(v,ct)})}
  function scheduleClipEnvelope(param,c,start,end,when){const points=dsp()?.clipAutomationPoints?.(c,start,end)||legacyClipPoints(c,start,end);scheduleParam(param,points,when,start)}
  function scheduleDucking(param,c,start,end,when){const points=dsp()?.duckAutomationPoints?.(c,state.audioTimeline.clips,start,end,state.mixSettings)||legacyDuckPoints(c,start,end);scheduleParam(param,points,when,start)}
  function duckFactor(now,c){return c?.type==='music'?duckFactorAt(now):1}

  function stopAllSources(){for(const item of [...activeSources]){try{item.source.stop()}catch{}try{item.source.disconnect()}catch{}try{item.clipGain.disconnect()}catch{}try{item.duckGain.disconnect()}catch{}activeSources.delete(item)}}
  function scheduleClip(c,playFrom,ctxStart,myGeneration){const t=trackFor(c);if(!t?.url)return;const buffer=bufferCache.get(trackKey(t));if(!buffer||myGeneration!==generation||!running)return;const ac=audioContext(),rate=rateFor(c);const fallback=()=>{const clipStart=Number(c.start)||0,clipEnd=clipStart+Math.max(0,Number(c.duration)||0);if(clipEnd<=playFrom)return null;const timelineStart=Math.max(playFrom,clipStart),localTimeline=Math.max(0,timelineStart-clipStart),sourceOffset=Math.max(0,(Number(c.sourceOffset)||0)+localTimeline*rate);if(sourceOffset>=buffer.duration)return null;const timelineDuration=Math.min(clipEnd-timelineStart,(buffer.duration-sourceOffset)/Math.max(.0001,rate));if(timelineDuration<=.001)return null;return{timelineStart,when:ctxStart+Math.max(0,timelineStart-playFrom),sourceOffset,timelineDuration,bufferPlayDuration:timelineDuration*rate}};const plan=timing()?.computeClipSchedule?timing().computeClipSchedule({clip:c,playFrom,contextStart:ctxStart,bufferDuration:buffer.duration,rate}):fallback();if(!plan)return;const source=ac.createBufferSource(),clipGain=ac.createGain(),duckGain=ac.createGain();source.buffer=buffer;source.playbackRate.setValueAtTime(rate,plan.when);scheduleClipEnvelope(clipGain.gain,c,plan.timelineStart,plan.timelineStart+plan.timelineDuration,plan.when);scheduleDucking(duckGain.gain,c,plan.timelineStart,plan.timelineStart+plan.timelineDuration,plan.when);source.connect(clipGain).connect(duckGain).connect(masterGain);const item={source,clipGain,duckGain,clipId:c.id,generation:myGeneration};activeSources.add(item);source.onended=()=>{activeSources.delete(item);try{source.disconnect();clipGain.disconnect();duckGain.disconnect()}catch{}};source.start(plan.when,plan.sourceOffset,plan.bufferPlayDuration)}
  async function scheduleAll(playFrom){ensure();const ac=audioContext();if(ac.state==='suspended')await ac.resume();const myGeneration=++generation;stopAllSources();timelineAnchor=Math.max(0,Number(playFrom)||0);contextAnchor=0;await preloadFor(timelineAnchor);if(myGeneration!==generation||!running)return false;const lead=Math.max(.10,(ac.baseLatency||0)+.05);contextAnchor=ac.currentTime+lead;for(const c of state.audioTimeline.clips)scheduleClip(c,timelineAnchor,contextAnchor,myGeneration);return true}
  async function startAt(playFrom=0){if(running||starting)stop();q('#audioPlayer')?.pause();running=true;starting=true;timelineAnchor=Math.max(0,Number(playFrom)||0);contextAnchor=0;try{const ok=await scheduleAll(timelineAnchor);starting=false;if(!ok){running=false;return false}return true}catch(err){starting=false;running=false;contextAnchor=0;console.error(err);throw err}}
  function currentTime(){if(!running||!ctx||starting||contextAnchor<=0)return Math.max(0,timelineAnchor);const ct=presentedContextTime();if(timing()?.timelineTime)return timing().timelineTime(ct,timelineAnchor,contextAnchor);return ct<=contextAnchor?Math.max(0,timelineAnchor):Math.max(0,timelineAnchor+(ct-contextAnchor))}
  function stop(){if(running&&ctx&&!starting&&contextAnchor>0)timelineAnchor=currentTime();running=false;starting=false;contextAnchor=0;generation++;stopAllSources()}
  async function seekTo(sec){const was=running;stop();timelineAnchor=Math.max(0,Number(sec)||0);if(was)return startAt(timelineAnchor);return true}
  async function refreshSchedule(){const was=running&&!starting;if(!was)return true;const at=currentTime();stop();return startAt(at)}
  function isRunning(){return running&&!starting&&contextAnchor>0}function isStarting(){return starting}function clearBuffers(){bufferCache.clear();decodePromises.clear()}
  function getClockInfo(){const ac=ctx;return{running,isRunning:isRunning(),starting,timelineAnchor,contextAnchor,currentTime:currentTime(),contextTime:ac?.currentTime||0,presentedContextTime:presentedContextTime(),baseLatency:ac?.baseLatency||0,outputLatency:ac?.outputLatency||0,bufferCount:bufferCache.size,activeSources:activeSources.size}}

  function selectedClip(){if(window.cheerAudioEditor?.getSelectedClip)return window.cheerAudioEditor.getSelectedClip();const id=q('.audio-clip.selected')?.dataset.clip;return id?(state.audioTimeline?.clips||[]).find(c=>c.id===id):null}
  function redraw(){window.cheerAudioEditor?.renderTimeline?.();setTimeout(refreshInspector,0)}
  function nextMusicClip(c){return(state.audioTimeline?.clips||[]).filter(x=>x.type==='music'&&x.id!==c.id&&Number(x.start)>=Number(c.start)).sort((a,b)=>a.start-b.start)[0]||null}
  function autoCrossfade(){const c=selectedClip();if(!c||c.type!=='music'){alert('Valitse ensin musiikkiclippi.');return}const next=nextMusicClip(c);if(!next){alert('Tämän clipin jälkeen ei ole seuraavaa musiikkiclippiä.');return}snapshot();const overlap=Math.min(.45,Math.max(.18,60/(Number(state.targetBpm)||147)*.8)),wantedDuration=(next.start-c.start)+overlap;c.duration=Math.max(.05,Math.min(wantedDuration,maxDurationFor(c,c.sourceOffset||0)));const actual=Math.max(0,c.start+c.duration-next.start);c.fadeOut=Math.max(.05,actual||overlap);next.fadeIn=Math.max(.05,actual||overlap);scheduleSave();redraw();refreshSchedule()}

  function addInspector(){if(q('#clipInspector'))return;const host=q('#audioWorkspace .audio-status');if(!host)return;const box=document.createElement('div');box.id='clipInspector';box.style.cssText='display:none;align-items:end;gap:10px;flex-wrap:wrap;margin-top:10px;padding:10px 12px;border:1px solid rgba(148,163,184,.18);border-radius:10px;background:rgba(15,23,42,.45)';box.innerHTML=`<strong style="margin-right:4px">Clipin säätö</strong><label>Voimakkuus <input id="clipVolume" type="range" min="0" max="1" step="0.01" value="1"><span id="clipVolumeText">100 %</span></label><label>Lähteen alku <input id="clipSourceOffset" type="number" min="0" step="0.01" value="0" style="width:82px"> s</label><label>Fade in <input id="clipFadeIn" type="number" min="0" max="10" step="0.05" value="0" style="width:70px"> s</label><label>Fade out <input id="clipFadeOut" type="number" min="0" max="10" step="0.05" value="0" style="width:70px"> s</label><button id="btnAutoCrossfade" class="mini-btn">↔ Siirtymä seuraavaan</button><span id="clipTempoInfo" style="opacity:.8"></span>`;host.insertAdjacentElement('afterend',box);const save=()=>{const c=selectedClip();if(!c)return;c.volume=clamp(Number(q('#clipVolume').value)||0,0,1);c.sourceOffset=Math.max(0,Number(q('#clipSourceOffset').value)||0);c.duration=Math.min(Number(c.duration)||0,maxDurationFor(c,c.sourceOffset));c.fadeIn=clamp(Number(q('#clipFadeIn').value)||0,0,Number(c.duration)||0);c.fadeOut=clamp(Number(q('#clipFadeOut').value)||0,0,Number(c.duration)||0);q('#clipVolumeText').textContent=`${Math.round(c.volume*100)} %`;scheduleSave();redraw();refreshSchedule()};q('#clipVolume').addEventListener('input',save);q('#clipSourceOffset').addEventListener('change',save);q('#clipFadeIn').addEventListener('change',save);q('#clipFadeOut').addEventListener('change',save);q('#btnAutoCrossfade').addEventListener('click',autoCrossfade)}
  function refreshInspector(){addInspector();const box=q('#clipInspector'),c=selectedClip();if(!box)return;if(!c){box.style.display='none';return}box.style.display='flex';const volume=Number.isFinite(Number(c.volume))?Number(c.volume):1;q('#clipVolume').value=String(volume);q('#clipVolumeText').textContent=`${Math.round(volume*100)} %`;q('#clipSourceOffset').value=String(Math.max(0,Number(c.sourceOffset)||0));q('#clipFadeIn').value=String(Math.max(0,Number(c.fadeIn)||0));q('#clipFadeOut').value=String(Math.max(0,Number(c.fadeOut)||0));q('#btnAutoCrossfade').style.display=c.type==='music'?'inline-flex':'none';const r=rateFor(c),a=state.trackAnalysis?.[c.sourceName],bpm=Number(a?.bpm||a?.autoBpm);q('#clipTempoInfo').textContent=c.type==='music'&&bpm?`${bpm.toFixed(1)} → ${state.targetBpm} BPM · ${r.toFixed(3)}× · AudioContext-kello`:(c.type==='music'?'Tempo: alkuperäinen · AudioContext-kello':'Nopeus 1.000× · AudioContext-kello')}
  function init(){ensure();addInspector();q('#timelineContent')?.addEventListener('pointerdown',()=>setTimeout(refreshInspector,0));window.addEventListener('pointerup',()=>setTimeout(refreshInspector,0));window.addEventListener('keydown',e=>{if(e.key==='Delete'||e.key==='Backspace')setTimeout(refreshInspector,0)});document.addEventListener('cheer-audio-restored',()=>{stop();clearBuffers()});window.addEventListener('beforeunload',stop);refreshInspector();window.cheerTimelineAudioEngine={startAt,start:startAt,stop,seekTo,currentTime,isRunning,isStarting,rateFor,refreshInspector,autoCrossfade,duckFactor,clearBuffers,getAudioContext:audioContext,preloadFor,getClockInfo,refreshSchedule}}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();